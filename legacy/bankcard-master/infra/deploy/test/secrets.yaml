apiVersion: v1
kind: Secret
metadata:
  name: core-db-creds-empbc
  namespace: emp-test
type: Opaque
data:
  # base64 values
  dbPass: {DB_PASSWORD_B64ENC}
  keystorePass: {keystorePass_B64ENC}
  aadClientId: {aadClientId_B64ENC}
  aadSecret: {aadSecret_B64ENC}
  #connectionPass: {CONNECTION_PWD_B64ENC}
